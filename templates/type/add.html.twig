{% extends 'base.html.twig' %}

{% block title %}Add Type Trick{% endblock %}

{% block body %}
<section id="add_typeTrick" class="masthead bg-primary">
    <div class="container bg-white py-5 rounded">
        <div class="row col-10">
            <div class="">
                <a href="{{ path('app_type') }}" class="btn btn-primary">< Previous page</a>
            </div>
        </div>
        <div class="row d-flex justify-content-center">
            <div class="col-12 col-lg-6">
                <div class="col">
                    <div class="row mb-2">
                        <h1 class="text-center">Add Type Trick</h1>
                        <hr class="divider" />
                    </div>
                    
                    {% include "core/flashbag.html.twig" %}

                    {{ form_start(typeTrickForm) }}
                    {{ form_row(typeTrickForm.name) }}
                    {{ form_row(typeTrickForm.description) }}
                    <button type="submit" class="btn btn-primary">Submit</button>
                    {{ form_end(typeTrickForm) }}
                </div>

            </div>

        </div>
    </div>
</section>

{% endblock %}