{% extends 'base.html.twig' %}

{% block title %}Forgot password{% endblock %}

{% block body %}
<section id="forgot" class="masthead bg-primary">
    <div class="container bg-white py-5 rounded">
        <div class="row d-flex justify-content-center">
            <div class="col-12 col-lg-6">
                <div class="row mb-2">
                    <h1 class="text-center">Forgot password</h1>
                    <hr class="divider" />
                </div>
                {% include "core/flashbag.html.twig" %}
                {{ form_start(forgotPasswordForm) }}
                {{ form_row(forgotPasswordForm.email, {'attr':{'placeholder':'Enter your email'}}) }}
                <button type="submit" class="btn btn-primary">Submit</button>
                {{ form_end(forgotPasswordForm) }}
            </div>
        </div>
    </div>
</section>
{% endblock %}